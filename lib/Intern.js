// TODO: Write code to define and export the Intern class.  HINT: This class should inherit from Employee.

// REQUIRE IN HERE

//employee class code is:
// class Employee {
//   constructor (Id, Name, Role, Email, otherInfo) {
//         this.Id = Id;
//         this.name = Name;
//         this.Role = Role;
//         this.email = Email;
//         this.otherInfo = otherInfo;
//     }

// }

//right now I have an otherInfo property (?) in the Employee class, is this going to get a School property in the intern subclass?

//is the below the only thing to export? No capitalisation? Syntax same for a subclass?
module.exports = intern;